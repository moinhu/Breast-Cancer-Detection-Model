# 🩺 Breast Cancer Detection Model

A deep learning-based project that uses Convolutional Neural Networks (CNNs) to detect breast cancer from microscopic images.  
The model classifies breast tissue images into **Benign (non-cancerous)** or **Malignant (cancerous)** categories.

---

## 🚀 Project Overview

Breast cancer detection through histopathological image analysis is a crucial medical task.  
This model leverages **TensorFlow** and **Keras** to automatically extract visual features and make predictions with high accuracy.

**Key Highlights:**
- Built using **Python**, **TensorFlow**, and **Keras**
- End-to-end workflow: data preprocessing → model training → evaluation → deployment
- Achieved high accuracy on test datasets
- Includes visualization of ROC curves and training history
